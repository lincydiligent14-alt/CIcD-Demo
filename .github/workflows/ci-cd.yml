name: CI/CD for Web App

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  DOCKER_IMAGE_NAME: cicd-web
  DOCKER_USERNAME_VALUE: ${{ secrets.DOCKER_USERNAME || github.repository_owner }}
  DOCKER_HUB_REPOSITORY: ${{ format('{0}/cicd-web', secrets.DOCKER_USERNAME || github.repository_owner) }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image for PRs
        if: github.ref != 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          tags: |
            ${{ env.DOCKER_IMAGE_NAME }}:test

      - name: Log in to Docker Hub
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME_VALUE }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        if: github.ref == 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.DOCKER_HUB_REPOSITORY }}:latest
            ${{ env.DOCKER_HUB_REPOSITORY }}:${{ github.sha }}

      - name: Show image push summary
        if: github.ref == 'refs/heads/main'
        run: |
          echo "Docker image pushed successfully"
          echo "Image: ${{ env.DOCKER_HUB_REPOSITORY }}:latest"

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: ${{ github.ref == 'refs/heads/main' }}

    steps:
      - name: Check deployment secrets
        run: |
          if [ -z "${{ secrets.DEPLOY_HOST }}" ] || [ -z "${{ secrets.DEPLOY_USERNAME }}" ] || [ -z "${{ secrets.DEPLOY_SSH_KEY }}" ]; then
            echo "Deployment secrets are not fully configured. Skipping deployment."
            exit 0
          fi

      - name: Deploy to remote server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USERNAME }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          script: |
            docker pull ${{ env.DOCKER_HUB_REPOSITORY }}:latest
            docker rm -f ${{ env.DOCKER_IMAGE_NAME }} || true
            docker run -d --name ${{ env.DOCKER_IMAGE_NAME }} -p 80:80 ${{ env.DOCKER_HUB_REPOSITORY }}:latest
